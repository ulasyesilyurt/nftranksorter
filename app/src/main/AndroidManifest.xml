<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NFTRankSorter">

        <!-- ⭐ Artık LAUNCHER olan SplashActivity -->
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MainActivity artık normal activity (istersen hiç kullanmayabilirsin bile) -->
        <activity
            android:name=".MainActivity"
            android:exported="true" />

        <!-- Koleksiyon listesi: Splash'ten Intent ile açılacak -->
        <activity
            android:name=".ui.collections.CollectionListActivity"
            android:exported="true" />

        <!-- Diğer ekranlar -->
        <activity android:name=".ui.nft.NFTListActivity" />
        <activity android:name=".ui.nft.NFTDetailActivity" />
        <activity android:name=".ui.nft.AddNftActivity" />
        <activity android:name=".ui.collections.AddCollectionActivity" />
    </application>

</manifest>